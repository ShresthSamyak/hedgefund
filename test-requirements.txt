# Minimal subset of requirements.txt for CI / pytest.
# Heavy ML deps (torch, transformers, ccxt, websockets, feedparser, yfinance,
# telegram, redis, anthropic, google-generativeai) are all lazy-imported by
# the agent code and not exercised by the unit tests.

pytest==8.3.4
pytest-asyncio==0.24.0
pytest-timeout==2.4.0

SQLAlchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.6.1

# Indicators + pairs math
numpy==2.1.3
statsmodels==0.14.4
pandas==2.2.3

# PDF report
reportlab==4.2.5

# Scheduler (imported by main; we just need the module to load)
apscheduler==3.10.4

# FastAPI testclient
fastapi==0.136.1
httpx==0.27.2

# Lint
ruff==0.8.3
